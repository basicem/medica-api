<!DOCTYPE html>
<html>
<head>
  <title>{{ subject }}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
    }
    h1 {
      color: #004a8e;
    }
    a {
      color: #1e90ff;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    .confirmation-btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #1e90ff;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
    }
    .note-section {
      margin-top: 20px;
      border-top: 1px solid #ccc;
      padding-top: 10px;
    }
  </style>
</head>
<body>
  <h1>{{ title }}</h1>
  <p>{{ message }}</p>
  <p><a class="confirmation-btn" href="{{ confirmationLink }}">Confirm Appointment</a></p>
  <div class="note-section">
    <h3>Please Note:</h3>
    <p>{{ note }}</p>
  </div>
</body>
</html>
